{
  "name": "context-prompt-assistant",
  "version": "0.2.0",
  "private": false,
  "type": "module",
  "description": "A Windows companion app and Codex plugin for context-aware prompt optimization.",
  "author": "Context Prompt Assistant contributors",
  "main": "desktop/main.cjs",
  "scripts": {
    "test": "node --test",
    "check": "node --check scripts/context.mjs && node --check scripts/codex-cli.mjs && node --check scripts/codex-app-server.mjs && node --check scripts/server.mjs && node --check scripts/optimizer.mjs && node --check desktop/main.cjs && node --check desktop/preload.cjs && node --check desktop/renderer.js",
    "validate": "node scripts/validate.mjs",
    "start": "electron .",
    "dev": "electron .",
    "package:win": "electron-builder --win nsis portable"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "electron": "^39.8.10",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.contextpromptassistant.desktop",
    "productName": "Context Prompt Assistant",
    "directories": { "output": "release" },
    "files": ["desktop/**/*", "scripts/**/*", "skills/**/*", ".codex-plugin/**/*", ".mcp.json", "README.md", "LICENSE", "CHANGELOG.md"],
    "win": { "target": ["nsis", "portable"] },
    "nsis": { "oneClick": false, "allowToChangeInstallationDirectory": true, "createDesktopShortcut": true, "artifactName": "${productName}-${version}-${arch}-setup.${ext}" },
    "portable": { "artifactName": "${productName}-${version}-${arch}-portable.${ext}" }
  }
}
